- name: configuring the mongodb server
  become: yes
  hosts: mongodb
  tasks:
  - name: copying mongo repo
    ansible.builtin.copy:
      src: mongo.repo
      dest: etc/yum.repos.d/mongo.repo
      